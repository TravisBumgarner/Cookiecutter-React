{
  "name": "{{ cookiecutter.project_name }}",  
  "author": "{{ cookiecutter.project_author }}",  
  "scripts": {
        "start-dev": "webpack serve --mode=development --config=webpack.js",
        "bundle-ui": " rm -rf ./public; mkdir ./public; webpack --mode=production --config=webpack.js"
      }
}